# Package layout note

This repository is the standalone `pDAL` package requested by the build brief, so its `include/pdal`, `src`, `config`, `tests`, and `docs` directories are rooted directly here rather than nested under an AVS workspace's `src/pdal` directory.

See [`../../README.md`](../../README.md) for build and use instructions.
